# ============================================================
# Inge Gräßle MdB Büro — Open WebUI
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openwebui-inge-graessle
  namespace: multi-org-rag
  labels:
    app: openwebui-inge-graessle
    org: inge-graessle
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openwebui-inge-graessle
  template:
    metadata:
      labels:
        app: openwebui-inge-graessle
        org: inge-graessle
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z postgres 5432; do echo "Waiting for postgres..."; sleep 2; done']
      containers:
        - name: openwebui
          image: ghcr.io/open-webui/open-webui:v0.8.12
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: openwebui-shared-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: multi-org-secrets
                  key: DATABASE_URL_IG_MDB
            - name: DEFAULT_MODELS
              valueFrom:
                configMapKeyRef:
                  name: multi-org-config
                  key: VLLM_MODEL_NAME
            - name: OPENAI_API_KEYS
              valueFrom:
                secretKeyRef:
                  name: multi-org-secrets
                  key: VLLM_API_KEY
            - name: RAG_EXTERNAL_RERANKER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: multi-org-secrets
                  key: RERANK_API_KEY
            - name: TAVILY_API_KEY
              valueFrom:
                secretKeyRef:
                  name: multi-org-secrets
                  key: TAVILY_API_KEY
            - name: WEBUI_NAME
              value: "Inge Gräßle MdB Büro"
            - name: WEBUI_URL
              value: "http://inge-graessle-mdb.localhost"
            - name: WEBUI_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: multi-org-secrets
                  key: SECRET_KEY_INGE_GRAESSLE_MDB
            - name: WEBUI_ADMIN_EMAIL
              valueFrom:
                secretKeyRef:
                  name: multi-org-secrets
                  key: WEBUI_ADMIN_EMAIL
            - name: WEBUI_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: multi-org-secrets
                  key: WEBUI_ADMIN_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /app/backend/data
          resources:
            requests:
              memory: "1Gi"
              cpu: "250m"
            limits:
              memory: "4Gi"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: openwebui-inge-graessle-data

---
apiVersion: v1
kind: Service
metadata:
  name: openwebui-inge-graessle
  namespace: multi-org-rag
spec:
  selector:
    app: openwebui-inge-graessle
  ports:
    - port: 8080
      targetPort: 8080
