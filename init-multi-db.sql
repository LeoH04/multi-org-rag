-- ============================================================
-- AIDAHO Open WebUI — Database Initialization
-- Runs only on FIRST startup (when postgres data volume is empty)
-- ============================================================

-- AIDAHO Copilot database
CREATE DATABASE openwebui_aidaho;
